[project]
name = "sport-bot"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = [
    "PyNaCl==1.5.0",
    "google-auth==2.29.0",
    "google-auth-httplib2==0.2.0",
    "google-api-python-client==2.126.0",
]

[tool.vercel]
entrypoint = "api.discord:handler"
